---
title: Line Chart
description: Build line charts with custom dots, labels, and multiple series
navigation:
  icon: i-lucide-trending-up
---

Line charts display data as a series of points connected by line segments.

## Simple Line Chart

A basic line chart with a single series.

::chart-demo{name="Simple Line Chart" description="A line chart with monotone interpolation." src="line-charts/simple-line-chart"}
::

## Multi-Line Chart

Add multiple `<Line>` components for multi-series charts.

::chart-demo{name="Multi-Line Chart" description="Two line series with different colors." src="line-charts/multi-line-chart"}
::

## Dashed Line Chart

Use `stroke-dasharray` for dashed lines.

::chart-demo{name="Dashed Line Chart" description="Two lines with different dash patterns." src="line-charts/dashed-line-chart"}
::

## Add dots to data points

Use the `dot` prop to show dots on every point:

```vue
<Line
  type="monotone"
  data-key="desktop"
  stroke="#8884d8"
  :dot="{ fill: '#8884d8' }"
  :active-dot="{ r: 6 }"
/>
```

## Add labels to lines

Place `<LabelList>` inside `<Line>`:

```vue
<Line type="monotone" data-key="desktop" stroke="#8884d8">
  <LabelList position="top" :offset="12" class="fill-foreground" :font-size="12" />
</Line>
```

## Line props

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `dataKey` | `string \| number \| Function` | **required** | Key from data |
| `type` | `CurveType` | `'linear'` | Curve type (`monotone`, `step`, `natural`, etc.) |
| `stroke` | `string` | `'#3182bd'` | Line stroke color |
| `strokeWidth` | `number` | `1` | Line width |
| `dot` | `boolean \| object` | `false` | Show dots on data points |
| `activeDot` | `boolean \| object` | `true` | Show active dot on hover |
| `connectNulls` | `boolean` | `false` | Connect across null values |
| `hide` | `boolean` | `false` | Hide the line |
| `isAnimationActive` | `boolean` | `true` | Enable animation |
