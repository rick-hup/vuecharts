---
title: Domain and Ticks
description: Configure axis range, data domain, and tick formatting
navigation:
  icon: i-lucide-ruler
---

The **domain** defines the min/max range of an axis. **Ticks** are the labeled markers along the axis.

The examples below use solar system data — planet masses span from 10²² kg (Pluto) to 10³⁰ kg (Sun), making domain configuration essential.

## Default domain

By default, axes calculate their domain from the data. When values span many orders of magnitude, the result is unsatisfactory — smaller planets are invisible because the Sun dominates:

::chart-demo{name="Default Domain" description="Sun dominates the Y axis — all other planets are barely visible." src="guide-charts/domain-default-chart"}
::

## YAxis as category type

Switching YAxis to `type="category"` shows each unique mass value as a discrete tick:

::chart-demo{name="Categorical Y Axis" description="YAxis type='category' shows each mass as a distinct tick." src="guide-charts/domain-category-axis-chart"}
::

## Custom domain with allowDataOverflow

Set a fixed domain to zoom into a specific range. Use `allow-data-overflow` so data outside the range still renders (overflowing the chart area):

::chart-demo{name="Custom Domain" description="Domain [1e23, 2e27] makes gas giants and rocky planets visible." src="guide-charts/domain-custom-chart"}
::

## Logarithmic scale

For data spanning many orders of magnitude, a logarithmic scale makes all values proportionally visible:

::chart-demo{name="Logarithmic Scale" description="Log scale reveals all planets across 8 orders of magnitude." src="guide-charts/domain-log-scale-chart"}
::

## Tick formatting

Use `tick-formatter` to display values in human-readable units. Here, mass in kg is converted to yottagrams (1 Yg = 10²⁴ kg):

::chart-demo{name="Tick Formatter" description="Axis ticks formatted as yottagrams instead of raw kg values." src="guide-charts/domain-tick-formatter-chart"}
::

## Domain options

```vue
<!-- Auto domain from data (default) -->
<YAxis />

<!-- Fixed domain -->
<YAxis :domain="[0, 100]" />

<!-- Data-driven bounds -->
<YAxis :domain="['dataMin', 'dataMax']" />

<!-- Auto with padding -->
<YAxis :domain="['auto', 'auto']" />

<!-- Custom function -->
<YAxis :domain="[(dataMin) => dataMin * 0.9, (dataMax) => dataMax * 1.1]" />
```
