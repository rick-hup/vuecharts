---
title: 安装
description: 如何安装 vccs 并开始使用 Vue 3 图表组件。
---

# 安装

## 安装依赖

```bash [pnpm]
pnpm add vccs
```

```bash [npm]
npm install vccs
```

```bash [yarn]
yarn add vccs
```

## 基本用法

导入图表组件并在 Vue 组件中使用：

```vue
<script setup>
import {
  BarChart,
  Bar,
  XAxis,
  YAxis,
  CartesianGrid,
  Tooltip,
  ResponsiveContainer,
} from 'vccs'

const data = [
  { name: 'Page A', uv: 4000, pv: 2400 },
  { name: 'Page B', uv: 3000, pv: 1398 },
  { name: 'Page C', uv: 2000, pv: 9800 },
  { name: 'Page D', uv: 2780, pv: 3908 },
  { name: 'Page E', uv: 1890, pv: 4800 },
  { name: 'Page F', uv: 2390, pv: 3800 },
]
</script>

<template>
  <ResponsiveContainer :width="'100%'" :height="300">
    <BarChart :data="data">
      <CartesianGrid stroke-dasharray="3 3" />
      <XAxis data-key="name" />
      <YAxis />
      <Tooltip />
      <Bar data-key="pv" fill="#8884d8" />
      <Bar data-key="uv" fill="#82ca9d" />
    </BarChart>
  </ResponsiveContainer>
</template>
```

## 使用 ResponsiveContainer

将图表包裹在 `ResponsiveContainer` 中使其自适应：

```vue
<template>
  <div style="width: 100%; height: 300px">
    <ResponsiveContainer>
      <BarChart :data="data">
        <!-- 图表内容 -->
      </BarChart>
    </ResponsiveContainer>
  </div>
</template>
```
