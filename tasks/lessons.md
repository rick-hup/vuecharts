# Lessons

## 2026-03-16 recent-pr-bugs 扫描

- PR 引入“分母来自数据极值”的几何计算时，必须先加 `maxValue <= 0` 防护；否则会直接把 SVG path 变成 `NaN`，渲染层会整段丢弃元素。
- 动画重构把内部组件参数升级成 `transition` 后，要同步检查外层公开组件 props 是否暴露并透传；否则是“无报错但配置失效”的静默回归。

